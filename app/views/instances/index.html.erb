<h1>Listing users</h1>

<table>
  <tr>
    <th>Server</th>
    <th>FQDN</th>
    <th></th>
  </tr>

<% Instance.all.each do |server| %>
  <tr>
    <td><%= server.name %></td>
    <td><%= server.canonical %></td>
    <td><%= server.location %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_instance_path %>
